{
  "name": "@yuiseki/osm2desc",
  "version": "0.0.1",
  "description": "Generate a text description from a node, a way or a relation on the OpenStreetMap",
  "main": "dist/index.js",
  "bin": {
    "osm2desc": "bin/osm2desc.js"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean; tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yui Matsumura <yuiseki@gmail.com> (https://twitter.com/yuiseki_)",
  "license": "WTFPL",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/yargs": "^17.0.18",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@turf/turf": "^7.0.0",
    "osmtogeojson": "^3.0.0-beta.5",
    "yargs": "^18.0.0"
  }
}
